# Core data handling
numpy>=1.24
pandas>=2.0
pyarrow>=14.0
pyyaml>=6.0
tqdm>=4.65

# Audio / speech analysis
praat-parselmouth>=0.4.3
librosa>=0.10
soundfile>=0.12

# Chinese NLP — for transcript → pinyin → tone in Stage 2
jieba>=0.42
pypinyin>=0.50

# Modeling and clustering
statsmodels>=0.14
scipy>=1.11
scikit-learn>=1.3
patsy>=0.5

# Plotting and notebooks
matplotlib>=3.7
seaborn>=0.12
jupyter>=1.0

# Tests
pytest>=7.4

# --- Installed separately (see README) ------------------------------------
# montreal-forced-aligner  — install via conda for Stage 3
# creapy                   — install from GitHub for Stage 8
